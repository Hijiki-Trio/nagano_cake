<main class="bacimage">
  <div class="title-box">
    <h3 class="title-text">ジャンル編集</h3> 
  </div>

    <div class="niw-item">
            <%= form_with model: [:admin, @genre], local:true do |f| %>
            <%= f.text_field :name, placeholder: "ジャンル名", size: "30", class: "form-control" %>
            <%= f.submit '変更を保存' %>
            <% end %>
    </div>
</main>